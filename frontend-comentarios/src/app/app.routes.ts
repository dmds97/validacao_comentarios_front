import { Routes } from '@angular/router';

export const routes: Routes = [
  // Adicione rotas adicionais aqui se necessário
  // Exemplo: { path: 'admin', loadComponent: () => import('./admin.component') }
];